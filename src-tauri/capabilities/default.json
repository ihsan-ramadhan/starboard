{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Lets the operator point a dataset at a workbook on disk or a department share.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
